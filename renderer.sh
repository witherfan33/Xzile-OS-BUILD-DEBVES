echo "opening x to z operations"

input "passwd"

if $passwd = 0 then echo "could not verify the users passwd"

  else
  
  input = 1 then echo "loading script" while do echo "downloading scripts" then sudo apt install -f
